@model DataArchives.Models.ViewModel.ArchiveIndexV

@{
    ViewData["Title"] = "Index";
    //Layout = "~/Areas/Identity/Pages/Account/Login.cshtml";
}



    <div class="tab-content">
        <div class="tab-pane fade show active" id="OwnDatasBlock" role="tabpanel" aria-labelledby="OwnDatasBlock-tab">
            @* вкладка с своими данными *@
            @if (Model.UserId != null)
            {
                @await Html.PartialAsync("OneSection",Model.MySection)
            }
            else
            {
                <h1>Вам необходимо авторизоваться</h1>
            }




        </div>
        <div class="tab-pane fade show" id="SearchBlock" role="tabpanel" aria-labelledby="SearchBlock-tab">
            <div class="spinner-border text-primary spinner-border-xl" role="status">
                <span class="sr-only">Loading...</span>
            </div>
            <div class="spinner-border text-primary" role="status">
                <span class="sr-only">Загрузка...</span>
            </div>
        </div>
        <div class="tab-pane fade show" id="ForegnDatasBlock" role="tabpanel" aria-labelledby="ForegnDatasBlock-tab">
            <div class="spinner-border text-primary spinner-border-xl" role="status">
                <span class="sr-only">Loading...</span>
            </div>
        </div>
      

    </div>

